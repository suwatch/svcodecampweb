// also supports: startTest(function(t) {
StartTest(function (t) {

    var Ext = t.Ext();
    var window = t.global;
    var document = window.document;
    
    // Running in the 'top' page scope. Get the local variables from the test.
    var Ext = t.Ext();
    var window = t.global;
    var document = window.document;

    t.click('>> #AttendeeSpeakerSponsorId #rbAttendee button', function () {
    });

   


})